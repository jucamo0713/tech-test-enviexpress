# Angular i18n Locales

Angular extracts marked template text into `messages.xlf`.

The starter translated locale is:

- `messages.es-CO.xlf`

Use:

```sh
pnpm run extract-i18n
```

Then copy new translation units from `messages.xlf` into the target locale files.

